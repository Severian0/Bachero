// Dashboard — docs/ARCHITECTURE.md §4. Live map (vehicles + potholes), detail panel,
// route planner, dispatch. Realtime on public.potholes (*) and public.vehicle_positions (INSERT).
export default function DashboardPage() {
  return (
    <main className="p-4">
      <h1 className="text-xl font-semibold">Bachero</h1>
      <p className="text-sm text-ink-55">Dashboard — not implemented yet.</p>
    </main>
  );
}
